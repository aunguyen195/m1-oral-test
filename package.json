{
  "name": "m1-oral-test",
  "version": "1.0.0",
  "description": "instant api by json-server",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "json-server --watch db.json --port 8080"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "json-server": "0.17.4"
  }
}
